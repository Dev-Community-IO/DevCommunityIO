/** Max characters allowed in profile bio (edit + display cap for new input). */
export const BIO_MAX_LENGTH = 280;

/** Collapsed bio line count on profile view. */
export const BIO_PREVIEW_LINES = 2;
