devcommunity-new-frontend

check the devcommunity frontend fuctionnalities and pages, analize and provide a plan for devcommunity-api.

do a full plan to implementing the frontend, will use postgres db, and aws s3 from storage.

the app must be very light, souple, leger, tres rapite et optimal, sans faille de securite, chaque post,page,.. avec un full seo generable automatiquement.

all variable in .env files not hardcoded variables,

search,filters, all must be perfect.
